services:
  - type: web
    name: kneemorph-api
    runtime: python
    rootDir: kneemorph-backend/kneemorph-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CORS_ORIGINS
        sync: false